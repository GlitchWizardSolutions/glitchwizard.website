 
cd /d "c:\xampp\htdocs\gws-universal-hybrid-app\public_html" && git status
  
# Check what's changed
git status

# Add all changes
git add .

# Commit with a message
git commit -m "Continuing replacing font awesome with bootstrap"

# Push to GitHub
git push origin main

#check status again
git status   